# SMM Bot — Project Roadmap

Дата создания: 03.07.2026  
Последнее обновление: 04.08.2026

---

# Цель проекта

Создать коммерческий Telegram-бот для помощи SMM-специалистам: вести клиентов, хранить идеи, готовить посты и создавать контент-планы.

Проект развивается законченными версиями. Базовый MVP завершён.

Этап 2 «Интеграция разделов» технически завершён.

Следующий основной этап — этап 3 «Архитектурное улучшение».

Во время OpenAI Build Week 2026 вне основного порядка была добавлена первая AI-функция — генерация семидневного контент-плана через OpenAI API. После конкурса проект вернулся к основному порядку разработки.

До этапа 4 новые AI-функции не добавляются. Разрешены только исправления ошибок, обеспечение безопасности и совместимости уже реализованного AI-контент-плана.

---

# Текущее состояние

- ✅ Базовый MVP всех четырёх разделов завершён.
- ✅ Этап 2 «Интеграция разделов» технически завершён.
- ✅ Раздел «Написать пост» связан с клиентами и сохранёнными идеями.
- ✅ Раздел «Контент-план» связан с клиентами и сохранёнными идеями.
- ✅ Клиентские сценарии переведены с глобальных флагов на FSM.
- ✅ Исправлены конфликты обработчиков и переходов «Назад».
- ✅ Команда `/start` очищает активный пользовательский сценарий.
- ✅ Добавлены интеграционные тесты этапа 2.
- ⬜ Архитектурное улучшение ещё не начато.
- 🟨 Первая AI-функция реализована досрочно.
- ⏸ Дальнейшее расширение AI отложено до этапа 4.
- ⬜ Коммерческий релиз и деплой не начаты.

Текущие версии основных разделов:

- 👥 Клиенты — v1.0, клиентские сценарии переведены на FSM.
- 💡 Идеи постов — v1.0, исправлена FSM-навигация и защита от устаревших номеров.
- ✍️ Написать пост — v2.0, интегрированный менеджер постов.
- 📅 Контент-план — v2.1, AI-генератор с контекстом клиента и выбранными идеями.

Git подключён, проект опубликован на GitHub. Завершённые изменения фиксируются отдельными коммитами и отправляются в удалённый репозиторий.

---

# Этап 1 — Базовый MVP

Статус: ✅ Завершён

## 👥 Клиенты — v1.0

Реализованы:

- добавление клиента;
- просмотр списка;
- карточка клиента;
- поиск;
- редактирование;
- удаление;
- проверка дубликатов.

Карточка клиента содержит:

- имя;
- фамилию;
- телефон;
- Instagram;
- email;
- заметки.

Данные хранятся в `clients.txt`.

## 💡 Идеи постов — v1.0

Реализованы:

- добавление идеи;
- просмотр списка;
- случайная идея;
- поиск;
- редактирование;
- удаление;
- проверка дубликатов.

Данные хранятся в `post_ideas.txt`.

## ✍️ Написать пост — базовый MVP v1.0

Реализованы:

- создание шаблонного поста;
- ввод клиента;
- выбор темы или идеи;
- выбор стиля;
- сохранение;
- история постов;
- поиск;
- удаление по ID.

Данные сохраняются в JSON-файл `posts.txt`.

## 📅 Контент-план — базовый MVP v1.0

Реализованы:

- создание статического плана по теме;
- сохранение;
- просмотр;
- поиск;
- редактирование;
- удаление.

Сценарии используют aiogram FSM. Данные хранятся в `content_plans.txt`.

---

# Этап 2 — Интеграция разделов

Статус: ✅ Технически завершён

Цель этапа — связать готовые разделы так, чтобы пользователь повторно использовал сохранённые данные, а не вводил одинаковую информацию вручную.

## ✍️ Написать пост — v2.0

Статус: ✅ Интеграция завершена

Реализовано:

- выбор сохранённого клиента кнопкой;
- отображение только имени и фамилии клиента;
- создание поста без клиента;
- выбор сохранённой идеи кнопкой;
- ввод собственной темы;
- сохранение копии карточки клиента вместе с постом;
- переходы «Назад» между этапами;
- обработка пустого списка клиентов;
- обработка пустого списка идей;
- проверка неправильного выбора;
- совместимость со старыми записями в `posts.txt`;
- отправка результатов поиска отдельными сообщениями.

Сохранены прежние функции:

- выбор стиля;
- создание шаблонного текста;
- история постов;
- поиск;
- удаление по ID.

Ручная проверка выполнена успешно. Ошибок в проверенных сценариях не обнаружено.

## 📅 Контент-план — v2.0

Статус: ✅ Интеграция с клиентами завершена

Реализовано:

- выбор сохранённого клиента кнопкой;
- создание плана без клиента;
- автоматическое добавление данных клиента в AI-бриф;
- использование имени, Instagram и заметок клиента;
- исключение телефона и email из AI-запроса;
- сохранение имени клиента в тексте готового плана;
- поиск плана по имени клиента;
- повторный выбор клиента при редактировании;
- переходы «Назад» между этапами;
- обработка пустого списка клиентов;
- проверка неправильного выбора;
- сохранение существующей логики безопасной AI-генерации.

Ручная проверка выполнена успешно.

## 📅 Контент-план — v2.1

Статус: ✅ Интеграция с идеями завершена

Реализовано:

- выбор одной или нескольких сохранённых идей;
- повторное нажатие снимает выбранную идею;
- отображение выбранных идей;
- возможность пропустить выбор идей;
- передача выбранных идей в AI-бриф;
- сохранение выбранных идей вместе с готовым планом;
- использование идей при создании и редактировании;
- mobile-first цифровая клавиатура;
- кнопки с номерами по три в строке;
- краткие списки контент-планов для редактирования и удаления;
- показ полного текста только после выбора плана;
- подтверждение удаления;
- отмена удаления;
- защита от устаревших номеров;
- проверка существования выбранных идей перед генерацией;
- повторная проверка идей после генерации перед сохранением;
- защита от выбора другой идеи при изменении файла.

Ручная проверка выполнена успешно.

## Исправления интеграции этапа 2

Во время итоговой технической проверки найдены и исправлены ошибки маршрутизации и FSM.

Исправлено:

- кнопка `⬅️ Назад` во всех FSM-сценариях раздела идей;
- конфликт общих обработчиков `Назад`;
- глобальные клиентские флаги заменены на FSM;
- пользовательские сценарии клиентов изолированы друг от друга;
- команда `/start` очищает активное FSM-состояние;
- устаревший номер редактируемой идеи больше не вызывает `IndexError`;
- редактирование идеи проверяет точную выбранную запись;
- контент-план использует снимок показанного списка идей;
- исчезнувшие и заменённые идеи проверяются перед продолжением;
- выбранные идеи повторно проверяются до и после AI-генерации.

## Уже завершённые связи

- основные роутеры объединены в одном Telegram-боте;
- раздел «Написать пост» использует сохранённых клиентов;
- раздел «Написать пост» использует сохранённые идеи;
- раздел «Контент-план» использует сохранённых клиентов;
- раздел «Контент-план» использует одну или несколько сохранённых идей;
- карточка клиента используется как контекст для AI-контент-плана;
- выбранные идеи используются как контекст для AI-контент-плана;
- пользовательские FSM-сценарии не конфликтуют между разделами;
- `/start` безопасно завершает незаконченный сценарий.

## Итоговая проверка этапа 2

Выполнено:

- проверка переходов между четырьмя разделами;
- проверка кнопки `⬅️ Назад`;
- проверка клиентского FSM;
- проверка независимости двух пользователей;
- проверка команды `/start`;
- проверка пустых файлов;
- проверка некорректных номеров;
- проверка устаревших записей;
- проверка перестановки и замены идей;
- проверка выбора идей для контент-плана;
- проверка создания и редактирования контент-плана;
- проверка компиляции всех Python-файлов;
- `git diff --check`;
- ручная проверка исправленных сценариев в Telegram.

Результат автоматических тестов:

```text
52/52 теста прошли успешно
```

Реальные запросы OpenAI во время автоматических тестов не выполнялись.

Известных технических препятствий для завершения этапа 2 не осталось.

## Не входит в этап 2

Следующие задачи переносятся на этап 3:

- уникальные ID клиентов и идей;
- устранение неоднозначности одинаковых имён;
- переход к структурированному хранилищу;
- SQLite;
- разделение бизнес-логики и handlers;
- устранение дублирования работы с файлами;
- единый подход к моделям данных;
- расширенная обработка ошибок;
- production-хранилище FSM;
- полноценная архитектура для нескольких пользователей.

---

# Этап 3 — Архитектурное улучшение

Статус: ⬜ Не начато

Цель этапа — улучшить внутреннее устройство проекта без добавления крупных пользовательских функций.

План:

- отделить бизнес-логику от Telegram handlers;
- вынести чтение и сохранение данных в отдельный слой;
- убрать дублирование функций работы с клиентами;
- убрать дублирование функций работы с файлами;
- проверить оставшиеся глобальные переменные и общие состояния;
- подготовить единые модели данных;
- добавить уникальные ID;
- исключить неоднозначность одинаковых имён и текстов;
- подготовить архитектуру к будущей смене хранилища;
- подготовить переход от TXT и JSON к SQLite;
- добавить единый подход к обработке ошибок;
- подготовить архитектуру к нескольким пользователям;
- определить production-хранилище FSM;
- расширить автоматические тесты остальных разделов.

На этом этапе SQLite не внедряется.

Цель этапа — подготовить модели данных, связи между сущностями и слой доступа к данным, чтобы переход на SQLite на коммерческом этапе прошёл безопасно.

Архитектурные изменения не должны добавлять новые пользовательские функции без отдельного решения.

---

# Этап 4 — Искусственный интеллект

Статус: 🟨 Частично выполнен досрочно, дальнейшая работа приостановлена

Во время OpenAI Build Week 2026 была реализована первая AI-функция проекта.

## 📅 AI-контент-план — реализовано

Текущая версия: v2.1

Реализовано:

- OpenAI Responses API;
- модель `gpt-5.6`;
- `reasoning.effort="low"`;
- Structured Outputs через Pydantic;
- ровно семь дней в порядке от 1 до 7;
- цель каждого дня;
- тема публикации;
- формат;
- ключевой тезис;
- CTA;
- пользовательский бриф до 500 символов;
- клиентский контекст;
- контекст одной или нескольких сохранённых идей;
- асинхронное ожидание без блокировки aiogram;
- единый AI-контракт для создания и редактирования;
- отсутствие сохранения при ошибке API;
- отсутствие замены существующего плана при ошибке;
- проверка выбранных идей перед и после AI-генерации;
- сообщения для ошибок авторизации, лимитов, сети, тайм-аута и сервиса;
- разделение длинных Telegram-сообщений;
- просмотр, поиск, редактирование и удаление планов.

## Проверка AI-функции

Завершены:

- компиляция обработчика;
- проверка OpenAI SDK;
- проверка Structured Outputs;
- проверка семидневной структуры;
- проверка порядка дней;
- проверка Telegram-форматирования;
- проверка отсутствия сохранения при ошибке;
- ручной live smoke test через Telegram;
- ручная проверка AI-генерации с выбранным клиентом;
- ручная проверка AI-генерации с выбранными идеями;
- проверка сохранения и поиска результата;
- проверка редактирования AI-плана;
- автоматические проверки выбранных идей до и после генерации.

## Следующие AI-функции

Отложены до активного начала этапа 4:

- AI-генерация идей постов;
- AI-генерация готовых постов;
- рерайт;
- улучшение текста;
- изменение стиля;
- генерация нескольких вариантов;
- использование полной карточки клиента в генерации постов;
- настройка нескольких AI-провайдеров;
- лимиты и учёт стоимости запросов;
- преобразование аудио в текст.

До этапа 4 новые AI-возможности не добавляются.

---

# Этап 5 — Коммерческая версия

Статус: ⬜ Не начато

Цель этапа — подготовить проект к реальному использованию несколькими пользователями и работе с постоянными данными.

План:

- внедрение SQLite;
- миграция данных из текущего хранения;
- профили пользователей;
- полная изоляция пользовательских данных;

---

# Текущие ограничения

- Данные хранятся в локальных TXT- и JSON-файлах.
- Нет базы данных и миграций.
- Нет уникальных ID клиентов и идей.
- При одинаковых именах клиентов возможна неоднозначность.
- При большом количестве клиентов и идей динамические клавиатуры станут слишком длинными.
- Нет пагинации.
- FSM использует временное хранилище и теряет незавершённые состояния после перезапуска.
- Автоматические тесты пока не покрывают весь проект одинаково подробно.
- Посты создаются по статическим шаблонам.
- Идеи постов не генерируются через AI.
- Нет публичного деплоя.
- Бот работает только при запущенном локальном процессе Python.
- Нет production-мониторинга и журналирования.
- Пользовательские данные пока не разделены по отдельным постоянным хранилищам.
- Текущий интерфейс ещё не прошёл полноценную проверку заказчицей.

---

# Ближайшие шаги

1. Обновить документацию после завершения этапа 2.
2. Обновить `02_LEARN_LATER.md`.
3. Обновить `PROJECT_LOG.md`.
4. Проверить и обновить `BACKLOG.md`.
5. Подготовить простой список заданий для проверки заказчицей.
6. Сохранить итоговую версию исправленных файлов в `code_history`, если это требуется правилами разделов.
7. Обновить соответствующие CHANGELOG.
8. Зафиксировать завершение этапа 2 отдельным Git-коммитом.
9. Отправить изменения на GitHub.
10. После фиксации этапа 2 начать этап 3 «Архитектурное улучшение».

---

# История основных версий

## Этап 1

- 👥 Клиенты — v1.0.
- 💡 Идеи постов — v1.0.
- ✍️ Написать пост — v1.0.
- 📅 Контент-план — v1.0.

## Опережающая AI-разработка

- 📅 Контент-план — v1.1, AI-генератор семидневного плана.

## Этап 2

- ✍️ Написать пост — v2.0, интеграция клиентов и идей.
- 📅 Контент-план — v2.0, интеграция клиентов и AI-контекста.
- 📅 Контент-план — v2.1, интеграция сохранённых идей и mobile-first интерфейс.
- 👥 Клиенты — перевод пользовательских сценариев на FSM.
- 💡 Идеи постов — исправление FSM-навигации и защиты от устаревших записей.
- 🧪 Интеграционные тесты этапа 2 — 52 успешных теста.

---

# Принцип развития

Каждая основная версия должна быть законченной, проверяемой и ограниченной по области изменений.

Во время основной разработки создаётся финальная рабочая версия раздела. Промежуточные версии могут быть заранее запланированы в `code_history`, но их код восстанавливается позже во время отдельных учебных сессий.

Перед крупным изменением предыдущая рабочая версия должна быть сохранена в `code_history`.

После создания нового или существенно обновлённого кода обязательно выполняется проверка:

- автоматические тесты;
- компиляция;
- `git diff --check`;
- основной пользовательский сценарий;
- альтернативные варианты;
- неправильный ввод;
- пустые данные;
- переходы «Назад»;
- команда `/start`;
- сохранение результата;
- поиск;
- редактирование;
- удаление, если оно предусмотрено;
- сценарии с устаревшими данными;
- отсутствие конфликтов между разделами.

После успешной проверки:

- версия сохраняется в `code_history`;
- обновляется разделовый `CHANGELOG.md`;
- обновляется проектная документация;
- изменения фиксируются отдельным Git-коммитом;
- коммит отправляется на GitHub.

Новые AI-функции добавляются только на этапе 4 и не должны ломать существующие CRUD-сценарии или сохранять неполный результат.

Обратная связь заказчицы используется для проверки:

- понятности интерфейса;
- удобства работы с телефона;
- полезности функций;
- качества получаемого результата.

Не каждое пожелание заказчицы автоматически становится задачей разработки. Изменения оцениваются по пользе, приоритету и соответствию текущему этапу ROADMAP.